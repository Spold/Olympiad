<Page x:Class="Olympiad.View.OlympiadListView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Olympiad.View"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="OlympiadListView">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>


        <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Right" Margin="10">
            <CheckBox x:Name="TutorCheckBox" 
        Content="Курируемые олимпиады"
        Margin="0 5 20 5"
      Checked="TutorCheckBox_Checked" Unchecked="TutorCheckBox_Unchecked" />

            <CheckBox x:Name="ParticipantCheckBox" 
                  Content="Мои олимпиады"
                  Margin="0 5 20 5"
                Checked="ParticipantCheckBox_Checked" Unchecked="ParticipantCheckBox_Unchecked" />

            <ComboBox x:Name="YearComboBox" 
                  Width="120"
                  SelectionChanged ="YearComboBox_SelectionChanged"
                  DisplayMemberPath="."/>
        </StackPanel>


        <ListView Name="OlympiadList" Grid.Row="1" ItemsSource="{Binding Olimparr}" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
            <ListView.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel Orientation="Horizontal" 
                    HorizontalAlignment="Center" Margin="50 0"/>
                </ItemsPanelTemplate>
            </ListView.ItemsPanel>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Border CornerRadius="10" 
                Background="#D9D9D9" 
                Margin="0 10"
                Width="190" 
                Height="120">
                        <StackPanel MouseLeftButtonDown ="StackPanel_MouseLeftButtonDown">
                            <TextBlock Text="{Binding Name}"
                    FontWeight="Bold"
                    Margin="0 10"
                    MaxWidth="180"
                    VerticalAlignment="Center" 
                    HorizontalAlignment="Center" 
                    TextAlignment="Center" 
                    TextWrapping="Wrap"/>

                            <TextBlock Text="Дата проведения:" 
                    VerticalAlignment="Center" 
                    HorizontalAlignment="Center" 
                    TextAlignment="Center"/>
                            <TextBlock Text="{Binding StartDate, StringFormat='dd.MM.yyyy'}"
                    VerticalAlignment="Center" 
                    HorizontalAlignment="Center"/>
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
